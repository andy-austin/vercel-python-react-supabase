{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m uvicorn index:handler --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'API build complete'"
  },
  "dependencies": {}
}
