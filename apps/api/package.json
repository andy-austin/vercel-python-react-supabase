{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd ../.. && python3 -m uvicorn apps.api.index:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'API build complete'"
  },
  "dependencies": {}
}
